feat(backend): implement repository layer with type-safe data access

- Add type definitions (enums, entities, repository types) in backend/src/types/
- Implement BaseRepository with CRUD, soft delete, and transaction support
- Create 7 specialized repositories (User, Workspace, Task, TaskAssignee, TaskDependency, Comment, Activity)
- Implement 5 service layers with Entity to DTO conversion pattern
- Add soft delete migration (003_add_soft_delete.sql) for tasks, comments, workspaces
- Create comprehensive documentation in docs/repository/ (README, architecture, backend, usage, migration)
- Implement business logic validation (1 workspace per user, circular dependency detection, dependency validation)
- Add dashboard service with ranking logic for task prioritization
- Support advanced queries with filtering, sorting, and pagination
- Integrate automatic activity logging in all service operations
- Maintain zero breaking changes - all code added alongside existing functionality

BREAKING CHANGE: None - This is an additive change. Migration required before use: run backend/src/migrations/003_add_soft_delete.sql
